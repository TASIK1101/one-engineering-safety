# 현장 파일럿 전 리스크 등록부

> **기준일**: 2026-05-28
> **빌드 상태**: ✅ `npm run build` 성공

---

## 리스크 등급 기준

| 등급 | 정의 |
|------|------|
| 🔴 **치명** | 해당 기능 자체가 동작하지 않아 파일럿 진행 불가 |
| 🟠 **높음** | 기능은 동작하나 데이터 손실 또는 핵심 흐름 차단 가능성 |
| 🟡 **중간** | 불편하거나 일부 환경에서 실패 가능, 우회 가능 |
| 🟢 **낮음** | UI 개선 수준, 기능에는 영향 없음 |

---

## 🔴 치명 리스크

### R-01. Supabase Storage 버킷 미설정
- **대상**: 사진 업로드 (안전점검·시정조치)
- **증상**: `safety-photos` 버킷이 Supabase에 생성되어 있지 않으면 업로드 즉시 실패
- **영향**: 사진 업로드 기능 전체 불가
- **확인 방법**: Supabase Dashboard → Storage → `safety-photos` 버킷 존재 여부 및 Public 정책 확인
- **해결**: 버킷 생성 + 공개 읽기 정책 설정 필요 (배포 전 필수)

### R-02. Supabase RLS 미설정으로 데이터 접근 불가
- **대상**: 전체 모듈
- **증상**: 테이블에 RLS가 활성화되어 있고 정책이 없으면 모든 INSERT/SELECT 실패
- **영향**: 데이터 저장·조회 전체 불가
- **확인 방법**: 각 테이블 RLS 정책 확인 (`work_permits`, `tbm_records`, `safety_inspections`, `corrective_actions` 등)
- **해결**: `supabase/migrations/001_init.sql` RLS 정책 적용 여부 검증

### R-03. 작업허가서 템플릿 데이터 없음
- **대상**: 작업허가서 작성
- **증상**: `work_permit_templates` 테이블에 데이터가 없으면 허가서 생성 자체 불가
- **영향**: 작업허가서 모듈 전체 사용 불가
- **확인 방법**: Supabase Table Editor → `work_permit_templates` 데이터 존재 여부
- **해결**: 시드 데이터 삽입 필요

---

## 🟠 높음 리스크

### R-04. 사진 URL이 새로고침 후 404 반환
- **대상**: 안전점검, 시정조치 사진
- **증상**: 업로드 직후에는 보이나 새로고침 시 이미지가 사라지거나 Vercel 404 반환
- **원인 가능성**: `buildPublicUrl()` 함수가 잘못된 URL을 조립하거나 Storage URL이 DB에 저장되지 않음
- **확인 방법**: 업로드 후 DB의 `before_photo_url` 컬럼 값을 직접 확인, Supabase Storage URL 형식과 일치하는지 대조
- **코드 위치**: `src/lib/photo-url.ts`, `src/components/ui/ImageUpload.tsx`

### R-05. 모바일 서명 Canvas 미동작
- **대상**: TBM 서명, 작업허가서 서명
- **증상**: iOS Safari에서 touchstart/touchmove 이벤트가 정상 캡처되지 않거나 서명이 떨려 보임
- **원인 가능성**: `react-signature-canvas` 라이브러리의 모바일 CSS, devicePixelRatio 처리
- **확인 방법**: 실제 iOS Safari + Android Chrome 각각 테스트
- **우회**: 별도 폰에서 테스트 후 재현 여부 확인

### R-06. QR 서명 링크가 내부 네트워크에서만 동작
- **대상**: TBM QR, 작업허가서 QR
- **증상**: QR 코드가 localhost 또는 내부 IP로 생성되면 외부 기기에서 접속 불가
- **원인 가능성**: `window.location.origin` 기반 URL 생성 시 개발 환경 혼용
- **확인 방법**: 프로덕션 배포 후 생성된 QR 링크의 도메인이 실제 Vercel URL인지 확인
- **코드 위치**: `src/components/tbm/TBMSignLinkBox.tsx`, `src/components/work-permit/WorkPermitSignLinkBox.tsx`

### R-07. 작업허가서 반려 후 재제출 시 체크리스트 상태 초기화 오류
- **대상**: 작업허가서 재제출 흐름
- **증상**: 반려 후 `WorkPermitResubmitForm`에서 체크리스트를 수정하고 재제출해도 DB에 반영되지 않을 수 있음
- **확인 방법**: 반려 → 재제출 → 상세 페이지에서 체크리스트 변경 사항 확인
- **코드 위치**: `src/components/work-permit/WorkPermitResubmitForm.tsx`, `/api/work-permits/resubmit`

### R-08. 작업자 직접 입력 서명 본인확인 우회 가능
- **대상**: 작업허가서 수동 입력 작업자 서명
- **증상**: 직접 입력 작업자는 이름·회사명만으로 서명 가능 → 아무나 서명 가능한 구조
- **영향**: 법적 증빙 효력 약화 (운영상 주의 필요)
- **현재 상태**: 의도된 설계일 수 있으나 현장 테스트에서 확인 필요

---

## 🟡 중간 리스크

### R-09. 인쇄 페이지 서명 이미지 미출력
- **대상**: TBM, 작업허가서, 교육 이수 인쇄 페이지
- **증상**: 브라우저 인쇄 시 서명 이미지(base64 또는 Supabase URL)가 출력되지 않을 수 있음
- **원인 가능성**: CSS `@media print`에서 이미지 숨김 처리, 외부 이미지 로드 타임아웃
- **확인 방법**: Chrome에서 인쇄 미리보기 후 이미지 포함 여부 확인

### R-10. 대용량 HEIC 사진 업로드 실패
- **대상**: 모바일 사진 업로드 (iOS)
- **증상**: iOS 기본 카메라는 HEIC 포맷으로 저장 → Supabase Storage 처리 가능하나 브라우저에서 미리보기 불가 가능
- **현재 코드**: `accept="image/jpeg,image/png,image/webp,image/heic,image/heif"` — HEIC 허용 중
- **확인 방법**: iOS에서 HEIC 사진 업로드 시 미리보기 표시 여부

### R-11. 빈 화면 처리 일부 미흡
- **대상**: 기록 보관함 탭, 일부 목록 페이지
- **증상**: Supabase 쿼리 결과가 null인 경우 에러 박스 표시는 추가했으나, 일부 하위 데이터(attendees, items 등)에서 null 처리 미흡 가능
- **코드 위치**: `records/page.tsx`, 각 모듈 목록 페이지

### R-12. 동시 승인 경쟁 조건
- **대상**: TBM 승인, 작업허가서 승인
- **증상**: 두 명의 관리자가 동시에 승인 버튼을 클릭하면 status가 중복 업데이트될 수 있음
- **영향**: 낮음 (소규모 회사 환경에서 동시 접속 가능성 낮음)
- **해결 방향**: Supabase의 `update ... where status = '검토중'` 조건부 업데이트로 방어 가능

---

## 🟢 낮음 리스크

### R-13. TypeScript `any` 타입 일부 존재
- **대상**: 코드 품질
- **영향**: 런타임 오류 가능성이 있지만 실제 실패로 이어질 가능성은 낮음
- **현재 상태**: `npm run build` TypeScript 검사 통과 중

### R-14. 모바일 폼 입력 시 키보드로 레이아웃 밀림
- **대상**: 안전점검 작성, 작업허가서 작성
- **증상**: iOS Safari에서 소프트 키보드 올라올 때 화면이 의도치 않게 스크롤
- **영향**: 사용 불가 수준은 아님, 불편함 수준

### R-15. 인쇄 시 배경색 인쇄 설정 필요
- **대상**: 인쇄 페이지 전반
- **증상**: 브라우저 기본값으로 배경색이 인쇄에서 제외될 수 있음 (상태 배지 색상 소실)
- **해결**: 인쇄 시 "배경 그래픽 인쇄" 옵션 활성화 안내 필요

---

## 배포 전 필수 체크리스트

파일럿 배포 전 반드시 해결해야 하는 항목:

- [ ] **R-01**: `safety-photos` 버킷 생성 + 공개 정책 설정
- [ ] **R-02**: 모든 테이블 RLS 정책 적용 확인
- [ ] **R-03**: `work_permit_templates` 시드 데이터 확인
- [ ] **R-06**: 프로덕션 URL에서 QR 링크 도메인 검증

파일럿 당일 모니터링할 항목:

- [ ] **R-04**: 사진 새로고침 후 유지 여부
- [ ] **R-05**: 모바일 서명 Canvas 동작 여부
- [ ] **R-07**: 반려 → 재제출 흐름 완주 여부
