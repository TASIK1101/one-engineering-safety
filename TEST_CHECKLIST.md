# 현장 파일럿 테스트 체크리스트

> **목적**: 실사용 전 핵심 기능이 정상 동작하는지 검증한다.
> **환경**: Supabase 연결된 프로덕션(또는 스테이징) 환경에서 수행한다.
> **테스터**: 관리자 계정 1개, 모바일 기기(근로자 역할) 1개 준비 필요

---

## 0. 사전 준비

- [ ] Vercel 또는 서버에 최신 코드 배포 완료
- [ ] Supabase 환경변수 설정 확인 (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`)
- [ ] 테스트용 관리자 계정 이메일/비밀번호 준비
- [ ] 테스트용 직원 2~3명 이름·전화번호 준비
- [ ] 모바일 기기에서 앱 URL 접속 가능 여부 사전 확인

---

## 1. 로그인 / 로그아웃

### T-01. 관리자 로그인

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 관리자가 이메일/비밀번호로 정상 로그인되는지 확인 |
| **테스트 절차** | 1. `/login` 접속<br>2. 이메일·비밀번호 입력<br>3. 로그인 버튼 클릭 |
| **기대 결과** | `/dashboard`로 이동, 네비게이션 메뉴 표시 |
| **실패 시 확인** | Supabase Auth 설정, 이메일 확인 여부, 환경변수 |

### T-02. 잘못된 비밀번호

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 잘못된 로그인 시 에러 메시지가 표시되는지 확인 |
| **테스트 절차** | 1. 틀린 비밀번호로 로그인 시도 |
| **기대 결과** | "이메일 또는 비밀번호가 올바르지 않습니다." 메시지 표시, 로그인 유지 안됨 |
| **실패 시 확인** | 로그인 에러 핸들링 코드 |

### T-03. 로그아웃

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 로그아웃 후 보호 페이지에 접근 불가한지 확인 |
| **테스트 절차** | 1. 네비게이션에서 로그아웃 클릭<br>2. 직접 `/dashboard` URL 접속 시도 |
| **기대 결과** | 로그아웃 후 `/login`으로 이동, 직접 접근 시도 시 `/login`으로 리다이렉트 |
| **실패 시 확인** | 미들웨어 설정, Supabase 세션 만료 처리 |

---

## 2. 교육 대상자 관리

### T-04. 직원 등록

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 직원(교육 대상자)을 등록할 수 있는지 확인 |
| **테스트 절차** | 1. `/employees/new` 접속<br>2. 이름·전화번호·부서 입력<br>3. 등록 버튼 클릭 |
| **기대 결과** | 직원 목록에 등록된 직원이 표시됨 |
| **실패 시 확인** | Supabase `employees` 테이블 권한(RLS), 로그인 세션 |

### T-05. 직원 목록 조회

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 등록된 직원 목록이 올바르게 표시되는지 확인 |
| **테스트 절차** | 1. `/employees` 접속 |
| **기대 결과** | 등록한 직원이 목록에 표시, 빈 경우 "등록된 직원이 없습니다" 표시 |
| **실패 시 확인** | DB 연결, admin_id 필터 |

### T-06. 직원 삭제

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 직원 삭제가 정상 동작하는지 확인 |
| **테스트 절차** | 1. 직원 상세 페이지 접속<br>2. 삭제 버튼 클릭<br>3. 목록 확인 |
| **기대 결과** | 삭제된 직원이 목록에서 사라짐 |
| **실패 시 확인** | RLS, cascade 삭제 설정 |

---

## 3. 안전교육 등록 / 서명 / 이수 처리

### T-07. 안전교육 생성

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 관리자가 교육 콘텐츠를 생성할 수 있는지 확인 |
| **테스트 절차** | 1. `/trainings/new` 접속<br>2. 교육 유형·제목·내용 입력<br>3. O/X 퀴즈 3개 입력<br>4. 저장 |
| **기대 결과** | 교육 목록에 생성된 교육 표시 |
| **실패 시 확인** | `trainings` 테이블 RLS, 퀴즈 JSON 저장 방식 |

### T-08. 교육 링크 생성 및 근로자 교육 수행

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 근로자에게 교육 링크를 보내고 교육이 완료되는지 확인 |
| **테스트 절차** | 1. 교육 상세 페이지에서 직원 선택 후 링크 생성<br>2. 모바일에서 링크 접속<br>3. 교육 내용 읽기<br>4. O/X 퀴즈 풀기<br>5. 전자서명 입력<br>6. 제출 |
| **기대 결과** | 이수 완료 처리, 관리자 화면에서 서명 이미지 + 완료 표시 |
| **실패 시 확인** | `training_assignments` 테이블, 서명 canvas 저장, 토큰 유효성 |

### T-09. 이수 확인 및 인쇄

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 이수 완료 후 인쇄용 페이지가 정상 출력되는지 확인 |
| **테스트 절차** | 1. `/print/training/[id]` 접속<br>2. 브라우저 인쇄(Ctrl+P) 실행 |
| **기대 결과** | 서명 이미지·이수자 명단이 포함된 인쇄용 레이아웃 표시 |
| **실패 시 확인** | 인쇄 CSS(print:block), 서명 이미지 로드 |

---

## 4. TBM (작업 전 위험성평가 교육)

### T-10. TBM 작성

| 항목 | 내용 |
|------|------|
| **테스트 목적** | TBM 레코드가 정상 생성되는지 확인 |
| **테스트 절차** | 1. `/tbm/new` 접속<br>2. 현장·공종·날짜·위험요인 입력<br>3. 저장 |
| **기대 결과** | TBM 목록에 "작성중" 상태로 항목 생성 |
| **실패 시 확인** | `tbm_records` 테이블, worksites 데이터 |

### T-11. TBM QR 서명 (모바일)

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 근로자가 QR 코드로 접속해 서명할 수 있는지 확인 |
| **테스트 절차** | 1. TBM 상세 페이지에서 QR 코드 표시<br>2. 모바일로 QR 스캔<br>3. 이름 + 전화번호 뒷자리 입력<br>4. 서명 입력 후 제출 |
| **기대 결과** | TBM 상세 페이지에서 서명완료 인원 카운트 증가, 서명 이미지 표시 |
| **실패 시 확인** | `sign_token` 생성, `/tbm/sign/[token]` 라우트, 본인확인 API |

### T-12. TBM 승인

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 관리자가 TBM을 최종 승인할 수 있는지 확인 |
| **테스트 절차** | 1. TBM 상세 페이지에서 승인 클릭 |
| **기대 결과** | 상태가 "완료"로 변경, 수정 불가 처리 |
| **실패 시 확인** | `/api/tbm/approve` 엔드포인트, status 업데이트 |

### T-13. TBM 출력

| 항목 | 내용 |
|------|------|
| **테스트 목적** | TBM 인쇄 페이지가 서명 포함 정상 출력되는지 확인 |
| **테스트 절차** | 1. `/records/tbm/[id]` 접속<br>2. 인쇄 실행 |
| **기대 결과** | 참석자 서명 이미지, 위험요인 목록, 날짜·현장 정보 모두 포함 |
| **실패 시 확인** | 서명 데이터(base64) 출력 여부, 인쇄 레이아웃 |

---

## 5. 안전점검

### T-14. 안전점검 작성

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 현장 점검 기록이 정상 저장되는지 확인 |
| **테스트 절차** | 1. `/inspections/new` 접속<br>2. 날짜·점검자·구역 입력<br>3. 카테고리별 점검 항목 양호/보통/불량 선택<br>4. 저장 |
| **기대 결과** | 점검 목록에 새 항목 생성, 불량 건수 표시 |
| **실패 시 확인** | `/api/inspections/create` 응답, `safety_inspections` 테이블 |

### T-15. 불량 항목 처리 및 사진 업로드

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 불량 항목에 사진을 업로드하고 저장되는지 확인 |
| **테스트 절차** | 1. 점검 항목에서 "불량" 선택<br>2. 불량 내용 입력<br>3. 사진 업로드(카메라 촬영 또는 갤러리 선택)<br>4. 업로드 완료 후 저장 |
| **기대 결과** | Supabase Storage에 사진 저장, 새로고침 후 사진 유지, "새 탭에서 사진 보기" 링크가 Supabase URL |
| **실패 시 확인** | `safety-photos` 버킷 존재 여부, Storage 공개 정책, `buildPublicUrl` 함수 |

### T-16. 시정조치 자동 생성 확인

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 불량 항목 저장 시 시정조치가 자동 생성되는지 확인 |
| **테스트 절차** | 1. 불량 항목 포함 점검 저장<br>2. `/corrective-actions` 접속 확인 |
| **기대 결과** | 불량 항목 개수만큼 시정조치 자동 생성, 출처 점검 정보 연결 |
| **실패 시 확인** | `create` API의 corrective_actions insert 로직 |

---

## 6. 시정조치

### T-17. 조치 결과 입력 및 After 사진 업로드

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 시정 담당자가 조치 결과와 사진을 등록할 수 있는지 확인 |
| **테스트 절차** | 1. 시정조치 상세 페이지 접속<br>2. 조치 결과 텍스트 입력<br>3. After 사진 업로드<br>4. "검토 요청" 제출 |
| **기대 결과** | 상태가 "검토중"으로 변경, After 사진이 Supabase URL로 저장 |
| **실패 시 확인** | `/api/corrective-actions/update` 엔드포인트, Storage 업로드 |

### T-18. 시정조치 승인 / 반려

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 관리자가 시정조치를 승인 또는 반려할 수 있는지 확인 |
| **테스트 절차** | 1. "검토중" 상태의 시정조치 상세 페이지 접속<br>2. 승인 클릭 → "완료" 확인<br>3. 다른 건에서 반려 + 사유 입력 → 반려 확인 |
| **기대 결과** | 승인 시 "완료", 반려 시 "반려" + 사유 표시 |
| **실패 시 확인** | `/api/corrective-actions/approve` 엔드포인트 |

---

## 7. 작업허가서

### T-19. 작업허가서 작성

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 관리자가 작업허가서를 생성할 수 있는지 확인 |
| **테스트 절차** | 1. `/work-permits/new` 접속<br>2. 등급(A/B) 선택<br>3. 허가 유형 선택 → 체크리스트 자동 채워짐 확인<br>4. 기본 정보 입력<br>5. 작업자 추가(직원 선택 + 수동 입력 각 1명)<br>6. 저장 |
| **기대 결과** | "서명중" 상태로 허가서 생성, 작업자 목록 표시 |
| **실패 시 확인** | `work_permit_templates` 데이터 존재 여부, `/api/work-permits/create` |

### T-20. 작업자 서명 (모바일)

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 작업자가 모바일에서 서명할 수 있는지 확인 |
| **테스트 절차** | 1. 허가서 상세에서 서명 QR / 링크 확인<br>2. 모바일로 접속<br>3. 이름 + 전화번호 뒷자리 입력(등록 직원)<br>4. 직접 입력 작업자는 회사명 + 이름 입력<br>5. 서명 후 제출 |
| **기대 결과** | 서명완료 카운트 증가, 서명 이미지 표시 |
| **실패 시 확인** | `/api/work-permits/verify-worker`, `/api/work-permits/sign-worker`, `permit_token` 유효성 |

### T-21. 작업허가서 반려 및 재제출

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 반려 → 수정 → 재제출 흐름이 정상 동작하는지 확인 |
| **테스트 절차** | 1. 검토중 상태에서 반려 + 사유 입력<br>2. 허가서 상세에서 반려 사유 확인<br>3. 체크리스트 수정 후 재제출<br>4. 상태 변경 확인 |
| **기대 결과** | 반려 후 "반려" 상태 + 사유 표시, 재제출 후 "검토중"으로 복귀 |
| **실패 시 확인** | `/api/work-permits/resubmit`, `WorkPermitResubmitForm` 컴포넌트 |

### T-22. 작업허가서 최종 승인

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 관리자가 작업허가서를 최종 승인할 수 있는지 확인 |
| **테스트 절차** | 1. "검토중" 상태에서 승인자 서명 입력 후 승인 클릭 |
| **기대 결과** | "승인완료" 상태로 변경, 수정·재서명 버튼 비활성화 |
| **실패 시 확인** | `/api/work-permits/approve` |

### T-23. 밀폐구역 허가서 특수 기능

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 밀폐구역 유형 선택 시 추가 섹션(가스 측정, 출입 대장)이 동작하는지 확인 |
| **테스트 절차** | 1. 밀폐구역 관련 유형으로 허가서 생성<br>2. 상세 페이지에서 가스 측정 기록 입력(O₂, 가연성 가스, CO)<br>3. 출입 관리대장에 입장/퇴장 시간 입력 |
| **기대 결과** | 가스 측정 기록이 시간 역순 표시, 출입 로그 저장 |
| **실패 시 확인** | `isConfinedSpace()` 판별 로직, 관련 API 엔드포인트 |

### T-24. 작업허가서 출력

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 승인완료된 허가서가 정상 출력되는지 확인 |
| **테스트 절차** | 1. 허가서 상세에서 🖨️ 출력 버튼 클릭<br>2. `/records/work-permit/[id]` 확인<br>3. 브라우저 인쇄 실행 |
| **기대 결과** | 체크리스트·작업자 서명·승인 이력 모두 포함, 서명 이미지 출력 |
| **실패 시 확인** | 인쇄 레이아웃 CSS, 서명 이미지(base64 또는 URL) 로드 |

---

## 8. 통합 기록 보관함

### T-25. 기록 조회 및 필터

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 각 모듈별 기록이 보관함에서 조회되는지 확인 |
| **테스트 절차** | 1. `/records` 접속<br>2. TBM·안전점검·시정조치·작업허가서 탭 각각 클릭<br>3. 날짜·상태 필터 적용 |
| **기대 결과** | 해당 모듈 기록이 필터 조건에 맞게 표시 |
| **실패 시 확인** | 각 모듈 데이터 조회 쿼리, 필터 상태 관리 |

### T-26. 기록 없을 때 빈 상태

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 데이터가 없을 때 빈 화면이 아닌 안내 메시지가 표시되는지 확인 |
| **테스트 절차** | 1. 기록이 없는 탭 또는 새 계정에서 각 목록 페이지 접속 |
| **기대 결과** | "기록이 없습니다" 또는 유사한 안내 메시지 표시 |
| **실패 시 확인** | 각 목록 페이지의 빈 상태 처리 코드 |

### T-27. 상세 기록 인쇄

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 보관함에서 인쇄 링크가 정상 동작하는지 확인 |
| **테스트 절차** | 1. 기록 목록에서 항목 클릭<br>2. 인쇄 버튼 클릭 |
| **기대 결과** | 인쇄 전용 페이지 또는 브라우저 인쇄 UI 정상 실행 |
| **실패 시 확인** | `/records/[모듈]/[id]` 라우트 |

---

## 9. 모바일 화면 테스트

### T-28. 모바일 반응형 레이아웃

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 주요 화면이 모바일(375px 기준)에서 사용 가능한지 확인 |
| **테스트 절차** | 1. 모바일 기기 또는 개발자 도구(375×812)에서 아래 페이지 접속<br>&nbsp;&nbsp;- 대시보드<br>&nbsp;&nbsp;- 안전점검 작성 폼<br>&nbsp;&nbsp;- TBM 서명 페이지<br>&nbsp;&nbsp;- 작업허가서 서명 페이지 |
| **기대 결과** | 버튼·입력칸이 잘리지 않고, 터치로 모든 기능 사용 가능 |
| **실패 시 확인** | 해당 페이지의 Tailwind 반응형 클래스(sm:/md:), overflow 처리 |

### T-29. 모바일 서명 Canvas

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 터치로 서명 입력이 정상 동작하는지 확인 |
| **테스트 절차** | 1. TBM 또는 작업허가서 서명 링크를 모바일에서 접속<br>2. 손가락으로 서명 Canvas에 서명<br>3. 제출 |
| **기대 결과** | 서명이 자연스럽게 입력되고 제출 후 서명 이미지가 저장됨 |
| **실패 시 확인** | `react-signature-canvas` 터치 이벤트, canvas 해상도(devicePixelRatio) |

### T-30. 모바일 사진 업로드

| 항목 | 내용 |
|------|------|
| **테스트 목적** | 모바일 카메라로 사진을 촬영해 업로드할 수 있는지 확인 |
| **테스트 절차** | 1. 안전점검 또는 시정조치 페이지에서 "카메라 촬영" 버튼 클릭<br>2. 카메라 앱으로 촬영<br>3. 업로드 완료 후 미리보기 확인 |
| **기대 결과** | 촬영 즉시 업로드, 미리보기 표시, Supabase URL 저장 |
| **실패 시 확인** | `capture="environment"` 속성, HEIC/HEIF 변환, Storage 업로드 |

---

## 테스트 완료 기준

| 모듈 | 핵심 흐름 | 결과 |
|------|-----------|------|
| 로그인/로그아웃 | T-01 ~ T-03 | ⬜ |
| 교육 대상자 | T-04 ~ T-06 | ⬜ |
| 안전교육 | T-07 ~ T-09 | ⬜ |
| TBM | T-10 ~ T-13 | ⬜ |
| 안전점검 | T-14 ~ T-16 | ⬜ |
| 시정조치 | T-17 ~ T-18 | ⬜ |
| 작업허가서 | T-19 ~ T-24 | ⬜ |
| 기록 보관함 | T-25 ~ T-27 | ⬜ |
| 모바일 | T-28 ~ T-30 | ⬜ |

**모든 ⬜ 항목이 ✅ 가 되면 현장 파일럿 배포 가능**
